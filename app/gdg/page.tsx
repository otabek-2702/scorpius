// STAGE 1 STUB — the early-access waitlist is planned but not built yet. See STAGE-PLAN.md.
import { ComingSoon } from "@/components/_stage1/ComingSoon";

export const metadata = { title: "Erta kirish · Scorpius" };

export default function GdgPage() {
  return (
    <ComingSoon
      titleUz="Erta kirish ro'yxati"
      descUz="Waitlist va jonli hisoblagich keyingi bosqichda ulanadi."
    />
  );
}
